/** Component for NotFound Page
 *  Renders a NotFound page for invalid URLs
 * 
 * Props:
 * - none 
 * 
 * State:
 * - none
 * 
 * CompanyDetail -> NotFound
 */

function NotFound() {
    return (
        <div className="NotFound">
            <p>The page you were looking for does not exist.</p>
        </div>
    );
}

export default NotFound;